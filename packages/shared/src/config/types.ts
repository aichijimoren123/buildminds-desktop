/**
 * Config Types (Browser-safe)
 *
 * Pure type definitions for configuration.
 * Re-exports from @claude-code-desktop/core for compatibility.
 */

// Re-export all config types from core (single source of truth)
export type {
    AuthType, McpAuthType, OAuthCredentials, Workspace
} from '@claude-code-desktop/core/types';

